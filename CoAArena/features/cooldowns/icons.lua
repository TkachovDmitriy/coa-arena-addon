-- cooldowns :: icons — cooldown icon widgets for the cooldowns domain.
local ADDON_NAME, ns = ...

local icons = {}
ns.cooldowns = ns.cooldowns or {}
ns.cooldowns.icons = icons

-- Create a cooldown icon frame parented to `parent`.
function icons.Create(parent)
   -- TODO: build the icon frame, cooldown swipe, and count text.
end
